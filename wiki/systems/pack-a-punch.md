# Pack-a-Punch

> Stub — content pending.

The Pack-a-Punch upgrade system: machine, animations, weapon variant swap, camo, and the second-tier `_up_up` chain Reapy is building for `zm_test`.

## TODO

- Vanilla PaP flow: trigger → animation → weapon swap → name change → camo
- The `pap_camo` weapon variant convention (suffix scheme)
- PaP machine prefab anatomy and FX
- Custom PaP machine model + script bundle hookup
- The `_up_up` second-tier chain Reapy authored — how it differs from vanilla, where it lives in `usermaps/zm_test/scripts/`
- PaP for community-pack weapons (HarryBo21, Skye ports): what their GDTs assume
- Interaction with attachments (sights, etc.)

## Research starting points

- YouTube corpus: `theme` includes `pap`.
- Reapy's `_up_up` implementation: `usermaps/zm_test/scripts/zm/`.
