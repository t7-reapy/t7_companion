# Prefabs

> Stub — content pending.

Reusable Radiant assemblies — geometry, entities, scripts, lights bundled into a `.map_prefab` (verify extension) that can be placed multiple times. Most community-pack content (perk machines, PaP, traps, dungeon kits) ships as prefabs.

## TODO

- Where prefabs live on disk (`map_source/_prefabs/`? verify) and how Radiant resolves them
- Creating a new prefab from a Radiant selection
- The "linked vs broken" prefab distinction — does BO3 Radiant have linked prefab updates?
- Editing a prefab post-placement: in-place vs round-trip through the prefab source
- Entities inside prefabs and their script targetnames — collision handling when placed multiple times
- Lights inside prefabs — bake implications
- Community-pack prefabs (HarryBo21 perk machines, dungeon kit, etc.) — what to expect when dropping them in

## Research starting points

- YouTube corpus: `theme` includes `prefabs`; metadata `dungeon-kit`.
